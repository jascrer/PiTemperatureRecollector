import mysql.connector

DATABASE_CONNECTION = {
    'user': '',
    'password': '',
    'database' : 'pi_temperatures',
    'raise_on_warnings': True
}

# connection = mysql.connector.connect(**DATABASE_CONNECTION)
# cursor = connection.cursor()
# cursor.close()
# connection.close()
